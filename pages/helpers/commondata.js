// All our images should have scaled versions with the following widths
const availableImageWidths = [100, 150, 200, 300, 400, 600];
const availableScreenDensities = [1, 2, 3];

module.exports.availableImageWidths = availableImageWidths;
module.exports.availableScreenDensities = availableScreenDensities;
